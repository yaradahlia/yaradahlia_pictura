package yd.pictura.persistence.annotations;

/**
 * Defines properties that are generated on remote persistence (like database server).
 * This kind of properties are generated after a persistence action.
 * And this kind of properties should not be important to application functionality, only to remote persistence functionality.
 * @author aaa
 */
public @interface NGenerated {
    
}
